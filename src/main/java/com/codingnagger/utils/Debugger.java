package com.codingnagger.utils;

public class Debugger {
    public static void writeDebugLine(String format, Object... params) {
//        try (var writer = new FileWriter("debug.txt", true)) {
//            writer.write(String.format("%s%n", String.format(format, params)));
//        } catch (IOException e) {
//            throw new RuntimeException(e);
//        }
    }
}
